# PAUL Handoff

**Date:** 2026-04-11
**Status:** paused

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** CineOrg — gestion de vidéothèque personnelle
**Core value:** Organiser et renommer automatiquement une vidéothèque personnelle à partir des téléchargements

---

## Current State

**Version:** 2.0.0-dev (Production & Maintenance Complète)
**Phase:** 40 of 6 (Intégration Jellyfin) — Not started
**Plan:** Not started
**Status:** Ready to plan

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop complete — ready for next PLAN]
```

---

## What Was Done

- **Cache mémoire série (perf)** : ajout d'un cache mémoire `dict[(titre_lower, année), candidats]` dans `pending_factory.py` pour éviter les recherches API TVDB et le scoring redondants quand plusieurs épisodes/saisons de la même série sont traités dans le même batch. Propagé dans `matching_step.py` (CLI) et `workflow.py` (web). Tests ajoutés (5 nouveaux tests). Commit `9ee14de`.

- **Favicon SVG** : création d'un favicon bobine de film (ambre sur fond sombre, cohérent avec la charte graphique) dans `src/web/static/favicon.svg`, référencé dans `base.html`. Commit `9ee14de`.

- **Sanitize titres séries pour dossiers** : le titre de la série n'était pas nettoyé (`sanitize_for_filesystem`) lors de la construction du nom de dossier dans `get_series_video_destination()` (organizer.py), contrairement aux noms de fichiers. Les caractères `:` et `?` causaient des problèmes de lecture sur le media center. Commit `f234e7c`.

- **Renommage dossiers existants** : 10 dossiers de séries corrigés manuellement dans `/media/Serveur/Collection/Series/` (9 avec `:` → `-`, 1 avec `?` → `...`).

---

## What's In Progress

- Rien en cours — tous les correctifs sont commités et poussés.

---

## What's Next

**Immediate:** Planifier Phase 40 (Intégration Jellyfin) avec `/paul:plan`

**Après :** Le tiret cadratin `–` (U+2013) dans les noms de dossiers séries pourrait poser problème (ex: "Luden – Les rois du quartier rouge"). L'utilisateur vérifie si son media center le gère. Si non, ajouter U+2013/U+2014 à `SPECIAL_CHARS_TO_DASH` dans `renamer.py`.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `src/services/workflow/pending_factory.py` | Factory matching avec cache série |
| `src/services/organizer.py` | Organisation fichiers — fix sanitize dossiers séries |
| `src/services/renamer.py` | Sanitize filesystem — `SPECIAL_CHARS_TO_DASH` |
| `src/web/static/favicon.svg` | Favicon bobine de film |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Check loop position
3. Run `/paul:resume` or `/paul:progress`

---

*Handoff created: 2026-04-11*
