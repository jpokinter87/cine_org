# PAUL Handoff

**Date:** 2026-04-14 15:12
**Status:** paused (inter-phases — session UX post-Phase 40)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** CineOrg — Application Python de gestion de vidéothèque personnelle
**Core value:** Organiser et renommer automatiquement une vidéothèque personnelle à partir des téléchargements, sans effort manuel.

Une session inter-phases s'est intercalée entre la clôture de Phase 40 et le démarrage de Phase 41 : amélioration UX ponctuelle (bulles d'aide contextuelle) sans impact sur la roadmap.

---

## Current State

**Version:** 2.1.0-dev (Lecteurs Externes & Intégrations)
**Phase:** 41 sur 2 — Intégration Jellyfin (**Not started**)
**Plan:** Aucun — Phase 40 complète, Phase 41 à planifier

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Phase 40 fermée — prêt pour /paul:plan Phase 41]
```

---

## What Was Done (cette session)

Session UX hors-phase : généralisation du pattern de bulles d'aide contextuelle.

- Création du macro Jinja partagé `src/web/templates/_macros.html` exposant `field_label(text, hint)` (labels de formulaire) et `hint_icon(hint)` (standalone, à côté d'un titre existant).
- Page config — section Lecteurs : 11 bulles sur les profils MPV/DuneHD (SSH, préfixes chemins local/distant, IP Dune, préfixes SMB films/séries, type, programme, cible…).
- Page config — autres sections : 13 bulles via le champ `hint` ajouté dans `_SECTIONS` (`src/web/routes/config.py`) sur Répertoires, Base de données, Clés API, Traitement, Journalisation.
- Page maintenance : 4 bulles sur les titres de cartes (Intégrité, Nettoyage, Sandbox, Corbeille) expliquant précisément ce que chaque traitement détecte/modifie et sa réversibilité.
- Styling `.field-hint` (icône `?` en `var(--accent-amber)`, 14px, hover `filter: brightness(1.25)`) + `.field-hint-bubble` (police body forcée, max-width 300px, ancrage intelligent selon le contexte).
- Bascule `toggleConfigSection()` dans `config/index.html` : `overflow: visible` après fin d'animation `max-height`, `overflow: hidden` au collapse — permet aux bulles de dépasser le cadre de section une fois déployée.
- Inversion ciblée `.maint-card-title .field-hint-bubble` (bulle sous l'icône au lieu d'au-dessus) pour éviter la troncature par le haut du viewport sur les cartes du haut de page.
- Cache-buster `asset_version()` dans `src/web/deps.py` (mtime `style.css`) + `base.html` `?v={{ asset_version() }}` — force le rechargement navigateur à chaque modif CSS.

**Tests :** 49 tests config/maintenance/player verts, ruff propre, rendu Jinja validé (33 bulles player + 12 config + 4 maintenance).

**Commit :** `c42258c feat(ui): bulles d'aide contextuelle sur config et maintenance` — pushed vers `origin/master`.

**Mémoire persistante** : `ui_hint_tooltips.md` (pattern + extension future possible à workflow/validation) + ligne dans `MEMORY.md`.

---

## What's In Progress

Rien.

---

## What's Next

**Immediate:** `/paul:plan` pour **Phase 41 — Intégration Jellyfin** (à lancer en session fraîche, comme déjà planifié par la session 2026-04-14 précédente).

**After that:** Exécution de la Phase 41 (volumes Docker, montage symlinks + cibles).

**Idée notée pour plus tard** : étendre le pattern de bulles d'aide aux pages workflow/validation si certains contrôles techniques y nécessitent des explications contextuelles.

---

## Key Files

| Fichier | Rôle |
|---------|------|
| `.paul/STATE.md` | État projet vivant |
| `.paul/ROADMAP.md` | Vue d'ensemble des phases |
| `.paul/PROJECT.md` | Définition projet et décisions |
| `.paul/phases/40-lecteur-dunehd/40-02-SUMMARY.md` | Dernier livrable Phase 40 |
| `src/web/templates/_macros.html` | Macros Jinja partagés pour tooltips (nouveau) |
| `src/web/static/css/style.css` | Styles `.field-hint*` + cache-busted via asset_version() |
| `src/web/deps.py` | Expose `asset_version()` dans les templates |

---

## Resume Instructions

1. Lire `.paul/STATE.md` pour la position courante
2. Lancer `/paul:resume` ou `/paul:progress`
3. Démarrer la planification de la Phase 41 avec `/paul:plan`

---

*Handoff créé : 2026-04-14 15:12*
