---
phase: 20-navigation-affichage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/templates/home.html
  - src/web/static/css/style.css
  - src/web/routes/library/browse.py
  - src/web/templates/library/_filters.html
autonomous: true
---

<objective>
## Goal
Améliorer la navigation et l'affichage de la bibliothèque : cartouches accueil cliquables, dédoublonnage des genres dans les filtres, et ajout du tri "récemment ajoutés".

## Purpose
Rendre la navigation plus fluide (accès direct depuis l'accueil) et corriger les irritants visuels (genres dupliqués EN/FR, pas de tri par date d'ajout).

## Output
- Cartouches accueil transformées en liens directs
- Liste des genres unifiée (français uniquement)
- Nouveau critère de tri "Récemment ajouté" dans la bibliothèque
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@src/web/templates/home.html (cartouches stats lignes 12-41)
@src/web/static/css/style.css (styles .stat-card)
@src/web/routes/library/browse.py (filtres genres lignes 220-231, tri lignes 176-211, items dict lignes 96-174)
@src/web/templates/library/_filters.html (select genre + sort)
</context>

<acceptance_criteria>

## AC-1: Cartouches accueil cliquables
```gherkin
Given la page d'accueil est affichée
When l'utilisateur clique sur la cartouche "Films"
Then il est redirigé vers /library/?type=movie

Given la page d'accueil est affichée
When l'utilisateur clique sur la cartouche "Séries"
Then il est redirigé vers /library/?type=series

Given la page d'accueil est affichée
When l'utilisateur clique sur la cartouche "En attente"
Then il est redirigé vers /validation
```

## AC-2: Genres dédoublonnés
```gherkin
Given la bibliothèque contient des genres en anglais (Drama, Family, Fantasy, Mystery) et en français (Drame, Familial, Fantastique, Mystère)
When l'utilisateur ouvre le filtre genre
Then seuls les termes français apparaissent (pas de doublons)
And le filtre avec le terme français capture aussi les entrées anglaises correspondantes
```

## AC-3: Tri récemment ajoutés
```gherkin
Given la bibliothèque est affichée
When l'utilisateur sélectionne "Tri: Récemment ajouté"
Then les films/séries sont triés par date d'ajout à la base (du plus récent au plus ancien par défaut)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Cartouches accueil cliquables</name>
  <files>src/web/templates/home.html, src/web/static/css/style.css</files>
  <action>
    Dans home.html, transformer les 4 `<div class="stat-card ...">` en `<a href="..." class="stat-card ...">` :
    - stat-films → href="/library/?type=movie"
    - stat-series → href="/library/?type=series"
    - stat-episodes → href="/library/" (pas de filtre spécifique, les épisodes sont dans les séries)
    - stat-pending → href="/validation"

    Dans style.css, ajouter les styles nécessaires pour que les `<a>` se comportent comme les `<div>` :
    - `a.stat-card { text-decoration: none; color: inherit; }`
    - Ajuster le hover existant pour inclure un effet visuel supplémentaire (ex. outline ou scale léger) indiquant que c'est cliquable
    - `cursor: pointer` sur `.stat-card`
  </action>
  <verify>Ouvrir http://localhost:8000/ — les 4 cartouches sont cliquables et mènent aux bonnes pages. Le style visuel reste identique avec un hover amélioré.</verify>
  <done>AC-1 satisfied: Films→/library/?type=movie, Séries→/library/?type=series, En attente→/validation</done>
</task>

<task type="auto">
  <name>Task 2: Dédoublonnage genres + tri récemment ajoutés</name>
  <files>src/web/routes/library/browse.py, src/web/templates/library/_filters.html</files>
  <action>
    **Dédoublonnage genres (browse.py) :**

    Ajouter un dictionnaire de normalisation EN→FR après la collecte des genres (après ligne 231) :
    ```python
    GENRE_NORMALIZE = {
        "Drama": "Drame",
        "Family": "Familial",
        "Fantasy": "Fantastique",
        "Mystery": "Mystère",
        "Action & Adventure": "Action",
        "War & Politics": "Guerre",
        "Science-Fiction & Fantastique": "Science-Fiction",
        "Kids": "Animation",
        "Soap": None,  # Exclure
        "Talk": None,   # Exclure
    }
    ```

    Appliquer la normalisation sur `all_genres` : remplacer chaque genre anglais par son équivalent français, supprimer les None.

    Modifier aussi le filtre genre dans les requêtes SQL (lignes 70, 134) : quand un genre français est sélectionné, chercher aussi les variantes anglaises correspondantes. Créer un helper `_genre_filter_variants(genre)` qui retourne la liste des variantes (ex. "Drame" → ["Drame", "Drama"]).

    **Tri récemment ajoutés (browse.py + _filters.html) :**

    Dans browse.py :
    - Ajouter `"created_at": m.created_at` dans le dict items pour les films (après ligne 114)
    - Ajouter `"created_at": s.created_at` dans le dict items pour les séries (après ligne 172)
    - Ajouter le cas de tri après le bloc `elif sort == "codec_audio"` (ligne 209) :
      ```python
      elif sort == "created_at":
          items.sort(
              key=lambda x: (x.get("created_at") or datetime.min, title_sort_key(x["title"])),
              reverse=descending,
          )
      ```
    - Importer `datetime` depuis le module standard si pas déjà importé

    Dans _filters.html :
    - Ajouter l'option dans le select sort :
      `<option value="created_at" ...>Tri: Récemment ajouté</option>`
    - Changer le `order` par défaut à "desc" quand sort=created_at (les plus récents en premier)
  </action>
  <verify>
    1. Ouvrir /library/ et vérifier que le dropdown genres ne contient plus Drama, Family, Fantasy, Mystery (seulement Drame, Familial, Fantastique, Mystère)
    2. Filtrer par "Drame" → doit inclure les films tagués "Drama" en base
    3. Sélectionner "Tri: Récemment ajouté" → les items les plus récents apparaissent en premier (ordre desc)
  </verify>
  <done>AC-2 et AC-3 satisfied : genres unifiés en français avec filtre correct, tri par date d'ajout fonctionnel</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/web/routes/library/detail.py (fiches détaillées — hors scope)
- src/infrastructure/persistence/models.py (modèles DB)
- src/utils/constants.py (constantes existantes)
- src/web/routes/home.py (route accueil — seul le template change)

## SCOPE LIMITS
- Le chemin S01E01 sur fiches séries est déjà implémenté (template series_detail.html lignes 142-146) — pas de modification nécessaire
- La cartouche "Épisodes" pointe vers /library/ sans filtre spécifique (les épisodes ne sont pas un type filtrable séparément)
- Les genres Soap, Talk sont exclus du filtre (rares, sans équivalent français pertinent)
- Ne pas modifier le mécanisme d'enrichissement TMDB/TVDB — on normalise uniquement à l'affichage

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Les 4 cartouches accueil sont cliquables et mènent aux bonnes URLs
- [ ] Le dropdown genres ne contient aucun doublon EN/FR
- [ ] Le filtre genre "Drame" capture aussi les entrées "Drama" en base
- [ ] Le tri "Récemment ajouté" fonctionne en ordre décroissant par défaut
- [ ] Le style visuel des cartouches reste cohérent (pas de changement de layout)
- [ ] uv run ruff check passe sans erreur
- [ ] uv run pytest passe (pas de régression)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Navigation accueil → bibliothèque fluide
- Genres propres sans doublons linguistiques
- Tri par date d'ajout opérationnel
</success_criteria>

<output>
After completion, create `.paul/phases/20-navigation-affichage/20-01-SUMMARY.md`
</output>
