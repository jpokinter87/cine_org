---
phase: 37-optimisations-sandbox
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/cleanup/analyzers.py
  - src/infrastructure/persistence/repositories/movie_repository.py
  - src/web/routes/maintenance.py
  - src/web/templates/maintenance.html
  - tests/unit/test_cross_genre_db.py
autonomous: true
---

<objective>
## Goal
Optimiser la détection cross-genre par pré-filtre DB et intégrer la gestion de la corbeille dans la page maintenance.

## Purpose
1. Éliminer le scan filesystem pour la détection cross-genre — requêter symlink_path en DB au lieu de rglob sur 5000+ fichiers
2. Rendre la corbeille accessible depuis la page maintenance (actuellement uniquement via /trash)

## Output
- `scan_cross_genre_duplicates()` requête la DB au lieu de parcourir le filesystem
- Section corbeille ajoutée à la page maintenance avec liste, restauration et vidage
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@src/services/cleanup/analyzers.py — scan_cross_genre_duplicates() actuel (2 passes filesystem, lignes 333-426)
@src/infrastructure/persistence/models.py — MovieModel.symlink_path (indexé), MovieModel.file_path
@src/infrastructure/persistence/repositories/movie_repository.py — méthodes existantes
@src/web/routes/maintenance.py — page maintenance SSE patterns
@src/web/routes/library/trash.py — page corbeille existante (GET /trash, POST restore, POST empty)
@src/web/templates/maintenance.html — template maintenance actuel
@src/infrastructure/persistence/repositories/trash_repository.py — TrashRepository CRUD
</context>

<acceptance_criteria>

## AC-1: Cross-genre via DB
```gherkin
Given des films avec symlink_path renseigné en DB pointant vers le même file_path mais dans des genres différents
When scan_cross_genre_duplicates() est appelé
Then les doublons cross-genre sont détectés sans parcourir le filesystem
And le résultat est identique au scan filesystem (même CrossGenreDuplicate)
```

## AC-2: Performance cross-genre
```gherkin
Given une vidéothèque avec 5000+ symlinks
When scan_cross_genre_duplicates() est appelé
Then la détection s'exécute via des requêtes DB indexées
And aucun rglob/readlink/resolve n'est effectué sur le filesystem
```

## AC-3: Corbeille sur page maintenance
```gherkin
Given des éléments supprimés dans la corbeille
When l'utilisateur accède à la page /maintenance
Then une section "Corbeille" affiche le nombre d'éléments et la liste
And l'utilisateur peut restaurer un élément individuel
And l'utilisateur peut vider la corbeille entièrement
And ces actions sont restreintes à localhost
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Optimisation scan_cross_genre_duplicates() par requête DB</name>
  <files>src/services/cleanup/analyzers.py, src/infrastructure/persistence/repositories/movie_repository.py, tests/unit/test_cross_genre_db.py</files>
  <action>
    Remplacer le scan filesystem dans `scan_cross_genre_duplicates()` par une approche DB :

    1. Dans movie_repository.py, ajouter une méthode `get_movies_with_symlink_grouped_by_filepath()` :
       - Requête : SELECT file_path, id, symlink_path FROM movies WHERE symlink_path IS NOT NULL AND file_path IS NOT NULL
       - Retourne un dict groupé par file_path (seuls les groupes avec 2+ entrées = doublons potentiels)

    2. Dans analyzers.py, modifier `scan_cross_genre_duplicates()` :
       - Appeler la nouvelle méthode du repository au lieu de parcourir le filesystem
       - Pour chaque groupe (même file_path, 2+ symlink_path différents) :
         - Extraire le genre depuis symlink_path avec `_extract_genre_folder()` (existant)
         - Appliquer `_get_genre_folder_priority()` (existant) pour déterminer le symlink à garder
         - Construire les CrossGenreDuplicate identiques au format actuel
       - Conserver le fallback filesystem si symlink_path n'est pas peuplé (< 50% des films ont symlink_path)

    3. Écrire des tests unitaires :
       - Test DB avec des MovieModel ayant même file_path mais symlink_path dans genres différents
       - Test que les CrossGenreDuplicate retournés sont corrects (keep/remove)
       - Test fallback filesystem quand symlink_path absent

    Éviter :
    - Ne PAS modifier les dataclasses CrossGenreDuplicate ni les executors
    - Ne PAS supprimer le code filesystem existant (garder en fallback)
    - Ne PAS modifier _extract_genre_folder() ni _get_genre_folder_priority() (déjà testés)
  </action>
  <verify>uv run pytest tests/unit/test_cross_genre_db.py -v && uv run pytest tests/ -k "cross_genre" -v</verify>
  <done>AC-1 et AC-2 satisfaits : doublons cross-genre détectés via DB, fallback filesystem disponible</done>
</task>

<task type="auto">
  <name>Task 2: Section corbeille dans la page maintenance</name>
  <files>src/web/routes/maintenance.py, src/web/templates/maintenance.html</files>
  <action>
    Ajouter une section "Corbeille" à la page maintenance :

    1. Dans maintenance.py, ajouter les routes :
       - Modifier la route GET /maintenance pour inclure les données corbeille (query TrashModel, count, liste)
       - POST /maintenance/trash/{trash_id}/restore — restaure un élément (réutiliser la logique de trash.py)
       - POST /maintenance/trash/empty — vider la corbeille (réutiliser la logique de trash.py)
       - Toutes ces routes sont localhost-only (pattern existant dans maintenance.py)

    2. Dans maintenance.html, ajouter la section corbeille :
       - Section dépliable "Corbeille (N éléments)" avec le même style que les sections existantes
       - Tableau : type (film/série), titre, date de suppression, raison
       - Bouton "Restaurer" par ligne (hx-post, hx-swap pour retirer la ligne)
       - Bouton "Vider la corbeille" global avec dialogue de confirmation overlay
       - Si la corbeille est vide, afficher "Corbeille vide" en gris

    3. Réutiliser les patterns existants :
       - Import TrashRepository depuis le container DI
       - Même pattern localhost-only que les routes delete existantes
       - Dialogue confirmation = même overlay custom que pour la suppression bibliothèque

    Éviter :
    - Ne PAS dupliquer la logique de restauration/vidage — importer depuis trash.py ou extraire en helper commun
    - Ne PAS modifier les routes existantes de /trash (elles restent fonctionnelles)
    - Ne PAS ajouter de CSS supplémentaire si les classes maintenance existantes suffisent
  </action>
  <verify>Lancer le serveur (uv run uvicorn src.web.app:app --reload), visiter /maintenance, vérifier que la section corbeille s'affiche avec les éléments, tester restauration et vidage</verify>
  <done>AC-3 satisfait : corbeille visible et interactive depuis la page maintenance</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/services/cleanup/dataclasses.py (CrossGenreDuplicate inchangé)
- src/services/cleanup/executors.py (fix_cross_genre_duplicates inchangé)
- src/web/routes/library/trash.py (page /trash existante reste fonctionnelle)
- src/web/routes/library/delete.py (flux de suppression inchangé)
- src/infrastructure/persistence/models.py (pas de modification des modèles)

## SCOPE LIMITS
- Pas de sandbox dans ce plan (Plan 02)
- Pas de modification du flux de suppression
- Pas de nouveau modèle DB
- Pas de nouvelle dépendance

</boundaries>

<verification>
Avant de déclarer le plan terminé :
- [ ] `uv run pytest tests/unit/test_cross_genre_db.py -v` passe
- [ ] `uv run pytest tests/ -k "cross_genre" -v` passe (tests existants pas cassés)
- [ ] `uv run pytest --tb=short` — pas de régression
- [ ] `uv run ruff check src/services/cleanup/analyzers.py src/web/routes/maintenance.py`
- [ ] Page /maintenance affiche la section corbeille
- [ ] Restauration et vidage fonctionnent depuis /maintenance
</verification>

<success_criteria>
- scan_cross_genre_duplicates() utilise la DB comme source primaire
- Fallback filesystem disponible si symlink_path non peuplé
- Section corbeille visible et fonctionnelle sur /maintenance
- Aucune régression sur les tests existants
</success_criteria>

<output>
Après complétion, créer `.paul/phases/37-optimisations-sandbox/37-01-SUMMARY.md`
</output>
