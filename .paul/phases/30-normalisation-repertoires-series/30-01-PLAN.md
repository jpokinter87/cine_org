---
phase: 30-normalisation-repertoires-series
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/organizer.py
  - src/services/cleanup/dataclasses.py
  - src/services/scanner.py
  - src/adapters/cli/helpers.py
  - src/adapters/cli/validation/batch_display.py
  - src/adapters/cli/repair/helpers.py
  - src/adapters/cli/commands/fix_symlinks_command.py
  - src/adapters/cli/commands/fix_bad_links_command.py
  - src/adapters/cli/commands/import_commands.py
  - tests/unit/adapters/cli/test_repair_helpers.py
  - tests/unit/test_association_checker.py
autonomous: true
---

<objective>
## Goal
Normaliser toutes les références aux répertoires séries dans le code source, passant des noms français accentués aux noms compatibles Jellyfin.

## Purpose
Préparer le code pour la structure Jellyfin. Une fois le code normalisé, le plan 02 pourra migrer les fichiers physiques et la DB en toute sécurité.

## Output
- Tous les fichiers source référencent les nouveaux noms de répertoires
- Les tests sont mis à jour et passent
- Le code compile et fonctionne (mais pointe vers la nouvelle structure qui n'existe pas encore physiquement — le plan 02 s'en charge)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@src/services/organizer.py (fonctions get_series_type, get_series_destination, get_series_video_destination)
@src/services/cleanup/dataclasses.py (constante MANAGED_SUBDIRS)
@src/services/scanner.py (variantes "Séries"/"Series" pour scan downloads)
@src/adapters/cli/helpers.py (affichage arbre transfert)
@src/adapters/cli/validation/batch_display.py (affichage arbre batch)
@src/adapters/cli/repair/helpers.py (extraction nom série depuis chemin)
@src/adapters/cli/commands/fix_symlinks_command.py (itération sous-répertoires)
@src/adapters/cli/commands/fix_bad_links_command.py (itération sous-répertoires)
@tests/unit/adapters/cli/test_repair_helpers.py (fixtures chemins)
@tests/unit/test_association_checker.py (fixtures chemins)
</context>

<acceptance_criteria>

## AC-1: Mapping des types séries normalisé
```gherkin
Given une série avec genre "Anime"
When get_series_type() est appelé
Then le type retourné est "Mangas"
And le chemin construit est video/Series/Mangas/...

Given une série avec genre "Animation"
When get_series_type() est appelé
Then le type retourné est "Animation"
And le chemin construit est video/Series/Animation/...

Given une série sans genre animation
When get_series_type() est appelé
Then le type retourné est "TV"
And le chemin construit est video/Series/TV/...
```

## AC-2: Séries documentaires routées vers Documentaires/
```gherkin
Given une série avec genre "Documentaire"
When get_series_type() est appelé
Then le type retourné est "Series documentaires"
And le chemin construit est video/Documentaires/Series documentaires/...
```

## AC-3: MANAGED_SUBDIRS inclut les nouvelles racines
```gherkin
Given le code cleanup qui itère les sous-répertoires gérés
When il vérifie si un répertoire est géré
Then "Films", "Series" et "Documentaires" sont reconnus
```

## AC-4: Scanner accepte les deux variantes (rétrocompat downloads)
```gherkin
Given un répertoire de téléchargements avec "Séries/" ou "Series/"
When le scanner parcourt les sources
Then les deux variantes sont reconnues comme MediaType.SERIES
```

## AC-5: Tous les tests passent avec les nouveaux chemins
```gherkin
Given les tests mis à jour avec les nouveaux noms de répertoires
When uv run pytest est exécuté
Then tous les tests passent
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Normaliser le cœur logique (organizer, cleanup, scanner)</name>
  <files>src/services/organizer.py, src/services/cleanup/dataclasses.py, src/services/scanner.py</files>
  <action>
    **organizer.py — get_series_type():**
    - Changer le retour par défaut de "Séries TV" → "TV"
    - Garder "Mangas" et "Animation" inchangés
    - Ajouter une détection de genre "Documentaire" → retourner "Series documentaires"
    - L'ordre de priorité : Anime→Mangas, Animation→Animation, Documentaire→Series documentaires, Autre→TV

    **organizer.py — get_series_video_destination():**
    - Ligne 529 : changer `video_dir / "Séries" / series_type` → logique conditionnelle :
      - Si series_type == "Series documentaires" : `video_dir / "Documentaires" / series_type`
      - Sinon : `video_dir / "Series" / series_type`
    - Mettre à jour les commentaires/docstrings (Séries → Series)

    **organizer.py — get_series_destination():**
    - Mettre à jour le commentaire ligne 478 : "storage/Series/{Type}/..."

    **cleanup/dataclasses.py:**
    - Changer MANAGED_SUBDIRS de ("Films", "Séries") → ("Films", "Series", "Documentaires")

    **scanner.py:**
    - Garder la double variante ["Séries", "Series"] pour rétrocompatibilité downloads
    - Pas de changement fonctionnel, juste vérifier que c'est déjà OK
  </action>
  <verify>uv run pytest tests/unit/services/test_organizer.py -v</verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfaits : types normalisés, documentaires routés, MANAGED_SUBDIRS à jour</done>
</task>

<task type="auto">
  <name>Task 2: Normaliser les adaptateurs CLI et web</name>
  <files>
    src/adapters/cli/helpers.py,
    src/adapters/cli/validation/batch_display.py,
    src/adapters/cli/repair/helpers.py,
    src/adapters/cli/commands/fix_symlinks_command.py,
    src/adapters/cli/commands/fix_bad_links_command.py,
    src/adapters/cli/commands/import_commands.py
  </files>
  <action>
    **cli/helpers.py:**
    - Ligne 257 : changer "Séries TV" → "TV"
    - Ligne 289 : changer "Séries/" → "Series/" dans le template Rich
    - Adapter la logique pour les séries documentaires (branche "Documentaires/" au lieu de "Series/")

    **cli/validation/batch_display.py:**
    - Ligne 242 : changer "Séries TV" → "TV"
    - Ligne 273 : changer "Séries/" → "Series/" dans le template Rich
    - Ligne 179/185 : changer la détection du répertoire racine "Séries" → "Series"
    - Adapter pour les séries documentaires (racine "Documentaires")

    **cli/repair/helpers.py:**
    - Ligne 36 : changer ("Séries TV", "Animation", "Mangas") → ("TV", "Animation", "Mangas")
    - Ligne 41 : changer `startswith("Séries ")` → `startswith("Series ")` pour détecter "Series documentaires"
    - Garder la détection séries/series (ligne 30) pour rétrocompatibilité des anciens chemins

    **commands/fix_symlinks_command.py:**
    - Ligne 68 : changer ("Films", "Séries") → ("Films", "Series", "Documentaires")

    **commands/fix_bad_links_command.py:**
    - Ligne 38 : changer ("Films", "Séries") → ("Films", "Series", "Documentaires")

    **commands/import_commands.py:**
    - Ligne 800 : mettre à jour le commentaire seulement
  </action>
  <verify>uv run pytest tests/unit/adapters/cli/ -v</verify>
  <done>AC-1, AC-2 satisfaits côté CLI : affichage et détection utilisent les nouveaux noms</done>
</task>

<task type="auto">
  <name>Task 3: Mettre à jour les tests</name>
  <files>
    tests/unit/adapters/cli/test_repair_helpers.py,
    tests/unit/test_association_checker.py,
    tests/unit/services/test_organizer.py
  </files>
  <action>
    **test_repair_helpers.py:**
    - Remplacer toutes les occurrences dans les fixtures de chemins :
      - "Séries/Séries TV/" → "Series/TV/"
      - "Séries/Animation" → "Series/Animation"
      - "Séries/Mangas" → "Series/Mangas"
      - "Séries/Séries documentaires" → "Documentaires/Series documentaires"
    - Adapter les assertions correspondantes

    **test_association_checker.py:**
    - Remplacer "/storage/Séries/" → "/storage/Series/" dans les fixtures

    **test_organizer.py:**
    - Ajouter un test pour get_series_type() avec genre "Documentaire" → "Series documentaires"
    - Vérifier que le test existant pour "Séries TV" passe maintenant avec "TV"
    - Ajouter un test pour le routage documentaire vers video/Documentaires/Series documentaires/

    Lancer la suite complète pour vérifier qu'aucune régression n'est introduite.
  </action>
  <verify>uv run pytest --tb=short -q</verify>
  <done>AC-5 satisfait : tous les tests passent avec les nouveaux noms de répertoires</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/infrastructure/persistence/models.py (les modèles DB ne changent pas — plan 02)
- src/infrastructure/persistence/database.py (pas de migration DB ici)
- src/services/transferer.py (le transfert utilise organizer, pas de changement direct)
- Les templates HTML (labels UI "Séries" restent en français pour l'affichage utilisateur)
- src/web/ (pas de changement côté web dans ce plan)

## SCOPE LIMITS
- Ce plan change UNIQUEMENT le code source et les tests
- PAS de renommage de fichiers physiques (storage/, video/) — c'est le plan 02
- PAS de mise à jour des file_path en DB — c'est le plan 02
- PAS de script de migration — c'est le plan 02

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest --tb=short -q` — tous les tests passent
- [ ] `uv run ruff check src/` — pas d'erreurs de lint
- [ ] `uv run ruff format --check src/ tests/` — formatage OK
- [ ] get_series_type() retourne "TV", "Animation", "Mangas", "Series documentaires"
- [ ] get_series_video_destination() route vers "Series/TV", "Series/Animation", "Series/Mangas", "Documentaires/Series documentaires"
- [ ] MANAGED_SUBDIRS = ("Films", "Series", "Documentaires")
</verification>

<success_criteria>
- Tous les tests passent
- Aucune référence à "Séries TV" ni "Séries" comme nom de répertoire dans le code source (sauf rétrocompat scanner)
- Le code est prêt pour le plan 02 de migration physique
</success_criteria>

<output>
After completion, create `.paul/phases/30-normalisation-repertoires-series/30-01-SUMMARY.md`
</output>
